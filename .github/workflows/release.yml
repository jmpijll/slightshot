name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: Version to build (without the leading v)
        required: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  release:
    name: Build, notarise and publish
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve version
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          fi

      - name: Import Developer ID certificate
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          echo "$MACOS_CERTIFICATE" | base64 --decode > /tmp/certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import /tmp/certificate.p12 -k build.keychain \
            -P "$MACOS_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" build.keychain
          rm -f /tmp/certificate.p12
          security find-identity -v -p codesigning

      - name: Build and sign the app
        env:
          VERSION: ${{ steps.version.outputs.version }}
          BUILD: ${{ github.run_number }}
          SPARKLE_PUBLIC_KEY: ${{ secrets.SPARKLE_PUBLIC_KEY }}
        run: ./Scripts/bundle.sh

      - name: Build the disk image
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          brew install create-dmg
          SIGN_IDENTITY="$(security find-identity -v -p codesigning \
            | awk -F'"' '/Developer ID Application/ {print $2; exit}')" \
            ./Scripts/make_dmg.sh

      - name: Notarise and staple
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
        run: ./Scripts/notarize.sh "build/Slightshot-${{ steps.version.outputs.version }}.dmg"

      - name: Generate the Sparkle appcast
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          SPARKLE_VERSION=2.10.0
          curl -fsSL -o /tmp/sparkle.tar.xz \
            "https://github.com/sparkle-project/Sparkle/releases/download/${SPARKLE_VERSION}/Sparkle-${SPARKLE_VERSION}.tar.xz"
          mkdir -p /tmp/sparkle && tar -xf /tmp/sparkle.tar.xz -C /tmp/sparkle

          mkdir -p dist
          cp "build/Slightshot-${VERSION}.dmg" dist/
          # Keep previously published entries so older versions can still update.
          curl -fsSL -o dist/appcast.xml \
            "https://jmpijll.github.io/slightshot/appcast.xml" || true

          printf '%s' "$SPARKLE_PRIVATE_KEY" > /tmp/sparkle_key
          /tmp/sparkle/bin/generate_appcast \
            --ed-key-file /tmp/sparkle_key \
            --download-url-prefix "https://github.com/${GITHUB_REPOSITORY}/releases/download/v${VERSION}/" \
            dist
          rm -f /tmp/sparkle_key
          cat dist/appcast.xml

      - name: Publish the GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Slightshot ${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: build/Slightshot-${{ steps.version.outputs.version }}.dmg

      - name: Prepare the update feed for Pages
        run: |
          mkdir -p public
          cp dist/appcast.xml public/appcast.xml
          cp docs/icon.png public/icon.png 2>/dev/null || true
          cat > public/index.html <<'HTML'
          <!doctype html><meta charset="utf-8">
          <title>Slightshot</title>
          <meta http-equiv="refresh" content="0; url=https://github.com/jmpijll/slightshot">
          HTML

      - uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy-feed:
    name: Deploy update feed
    needs: release
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
